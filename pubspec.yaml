name: module_architecture_flutter
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  # packages
  merge_dependencies:
    path: packages/merge_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  analysis_lints: ^1.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/AppIcons.ttf

  fonts:
    - family: AppIcons
      fonts:
        - asset: assets/fonts/AppIcons.ttf
