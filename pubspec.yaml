name: module_architecture_mobile
version: 1.0.0+1
publish_to: "none"
description: "A new Flutter project."

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  # packages
  merge_dependencies:
    path: packages/merge_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  analysis_lints: ^1.0.3

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/AppIcons.ttf

  fonts:
    - family: AppIcons
      fonts:
        - asset: assets/fonts/AppIcons.ttf
