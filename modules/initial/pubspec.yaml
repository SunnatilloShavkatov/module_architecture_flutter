name: initial
version: 0.0.1
homepage: "none"
publish_to: "none"
description: "A new Flutter project."

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.1.0
  # packages
  core:
    path: ../../packages/core
  components:
    path: ../../packages/components
  navigation:
    path: ../../packages/navigation
  platform_methods:
    path: ../../packages/platform_methods

dev_dependencies:
  flutter_test:
    sdk: flutter
  analysis_lints: ^1.1.0

flutter:
  uses-material-design: true