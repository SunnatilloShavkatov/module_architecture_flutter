name: navigation
description: "A new Flutter project."
version: 0.0.1
homepage: "none"
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  # external packages
  sheet:
    git:
      url: https://github.com/SunnatilloShavkatov/sheet.git
      ref: main
  chuck_interceptor: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
