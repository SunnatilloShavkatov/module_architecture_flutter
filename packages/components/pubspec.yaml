name: components
version: 0.0.1
homepage: "none"
publish_to: "none"
description: "A new Flutter project."

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  # intl  
  intl: any
  # rx
  rxdart: ^0.28.0
  # cached network image
  cached_network_image_ce: ^4.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  analysis_lints: any

flutter:
  uses-material-design: true
