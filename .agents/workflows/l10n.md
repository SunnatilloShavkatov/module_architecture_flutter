# /l10n

Qoida: `docs/rules/l10n.md`.

Fayllar (qidirma, shu uchtasi):
`packages/core/lib/src/l10n/app_uz.arb`, `app_ru.arb`, `app_en.arb`

Qadamlar:
1. Key'ni **uchalasiga ham** qo'sh, mavjud tartibni buzma.
2. Ishlatilgan joyda `context.l10n.<key>`.
3. Hech qanday literal matn qolmasin (`arch-guard` `l10n-text` bloklaydi).

`flutter gen-l10n` ni o'zing ishga tushirma — dasturchi qiladi.

Topshiriq foydalanuvchidan olinadi.

Qoidalar manbasi: `AGENTS.md`. Turn ichida analyze/test/pub get yo'q (§7).
